package com.ud.parcial2
/*
import com.ud.parcial2.model.PujaRequest
import com.ud.parcial2.repository.SubastaRepository
import io.mockk.coEvery
import io.mockk.mockk
import kotlinx.coroutines.test.runTest
import org.junit.Assert.assertTrue
import org.junit.Test
import retrofit2.Response

class SubastaRepositoryTest {

    @Test
    fun `cuando se envía una puja válida, retorna éxito`() = runTest {
        // Mock del repositorio
        val repo = mockk<SubastaRepository>()

        // Comportamiento simulado del mock
        coEvery { repo.enviarPuja(any(), any()) } returns Response.success(Unit)

        // Acción
        val result = repo.enviarPuja(1, PujaRequest("Daniel", 500.0))

        // Afirmación
        assertTrue(result.isSuccessful)
    }
}*/
